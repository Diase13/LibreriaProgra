@model IEnumerable<LibreriaProgra.Models.Usuario>

  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@10"></script>

<h2 style="text-align: center;">Lista de Usuario</h2>

<p>
    <button class="btn btn-primary"class="btn btn-success" onclick="window.location='@Url.Action("Create", "Usuario")'">Registrar</button>
</p>
<table class="table table-hover" style="text-align: center;">
    <thead class="thead-dark" style="text-align: center;">
        <tr>
                <th scope="col">
                    #
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.nombre)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.apellidos)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.edad)
                </th>
                <th scope="col">
                    @Html.DisplayNameFor(model => model.distrito)
                </th>
        </tr>
    </thead>

    <tbody>

    @foreach (var item in Model)
    {
        <tr>
            <th scope="row">
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                 <a asp-action="Delete" asp-route-id="@item.ID">Delete</a> |

            </th>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.apellidos)
            </td>
               <td>
                @Html.DisplayFor(modelItem => item.edad)
            </td>
               <td>
                @Html.DisplayFor(modelItem => item.distrito)
            </td>
        </tr>
    }        
    </tbody>    
</table>




@if ((String)TempData["prueba"] == "prueba01")
{
    <input value="hola" id="hola01" type="hidden">
}

@if ((String)TempData["prueba02"] == "prueba02") 
{
    <input value="hola" id="prueba02" type="hidden">
}

    <script>
        
        var x = document.getElementById('hola01').value;

        if(x == "hola") {
        
            Swal.fire(
                'Registro exitoso',
                'El usuario ha sido registrado exitosamente',
                'success'
            )
  
        }

       

    </script>

   <script>
        
        var y = document.getElementById('prueba02').value;

        if(y == "hola") {

            Swal.fire(
                'Editado correctamente',
                'El usuario se ha editado correctamente',
                'success'
            )
        }
   </script> 

